<%= turbo_stream.update @bundle %>

<%#= turbo_stream.replace @bundle, "" %>
<%= turbo_stream.remove @bundle %>

<% if (prev = @bundle.prev) %>
  <%= turbo_stream.after prev, @bundle %>
<% else %>
  <%= turbo_stream.prepend "bundles", @bundle %>
<% end %>

<%= turbo_stream_flash %>